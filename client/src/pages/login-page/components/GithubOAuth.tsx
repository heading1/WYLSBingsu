import Wrapper from './GithubOAuthStyle';

const GithubOAuth: React.FC = () => {
  return (
    <Wrapper disabled>
      <span>Github Login이 추후 제공될 예정입니다!</span>
    </Wrapper>
  );
};

export default GithubOAuth;
